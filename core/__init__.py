"""Pure primitives. Zero domain knowledge, zero dependencies on `domain/`.

`core/` imports nothing from the project outside `core/` itself (CLAUDE.md §3.1).
Checked by `tools/check_domain_purity.py`.
"""
